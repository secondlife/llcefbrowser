# single-browser-example

Renders one CEF browser offscreen into an OpenGL texture on a GLFW window, with mouse/keyboard input forwarded to the page and a JavaScript bridge (`window.cefQuery`) wired up. The simplest possible starting point for embedding a single browser instance.
